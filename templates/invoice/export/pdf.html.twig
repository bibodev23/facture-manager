<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facture n°{{ invoice.number }}</title>

    <style>
    /*
 * Les styles généraux pour la page entière
 */
body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    color: #333;
    max-width: 800px;
    margin: 40px auto;
    padding: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    background-color: #fff;
    font-size: 14px;
}

/*
 * L'en-tête de la facture : titre et numéro
 */
.header {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid #eee;
    padding-bottom: 10px;
    margin-bottom: 30px;
}

.header h1 {
    font-size: 28px;
    color: #1a1a1a;
    margin: 0;
}

.header p {
    font-size: 18px;
    color: #666;
    font-weight: bold;
    margin: 0;
}

/*
 * Les adresses de l'entreprise et du client
 */
.address {
    display: flex;
    justify-content: space-between;
    margin-bottom: 30px;
}

.company, .customer {
    background-color: #f9f9f9;
    padding: 15px;
    border-radius: 5px;
    width: 48%; /* pour un alignement propre côte à côte */
}

.address p {
    margin: 0;
    font-size: 13px;
}

.address p:first-child {
    font-weight: bold;
    margin-bottom: 5px;
    color: #444;
}

/*
 * Les dates et l'IBAN
 */
.date {
    background-color: #fff;
    border: 1px solid #ddd;
    padding: 15px;
    border-left: 4px solid #007BFF; /* Une touche de couleur pour attirer l'oeil */
    margin-bottom: 30px;
}

.date p {
    margin: 5px 0;
    font-size: 13px;
}

/*
 * Le tableau des prestations
 */
.deliveries {
    margin-bottom: 30px;
}

.table {
    width: 100%;
    border-collapse: collapse;
    text-align: left;
}

.table th, .table td {
    padding: 12px 15px;
    border-bottom: 1px solid #ddd;
}

.table th {
    background-color: #007BFF;
    color: #fff;
    font-weight: bold;
    text-transform: uppercase;
    font-size: 12px;
}

.table tbody tr:nth-child(even) {
    background-color: #f9f9f9;
}

.table td:last-child {
    text-align: right;
    font-weight: bold;
}

/*
 * Le bloc total
 */
.total {
    text-align: right;
    margin-top: 20px;
    padding-top: 10px;
    border-top: 2px solid #007BFF; /* Mettre l'accent sur le montant final */
}

.total p {
    margin: 5px 0;
    font-size: 16px;
    font-weight: bold;
}

.total span {
    display: inline-block;
    width: 120px;
    text-align: right;
}

/* Le grand total TTC */
.total p:last-child {
    font-size: 20px;
    color: #007BFF;
}

/*
 * Les notes et le pied de page
 */
.notes, .footer {
    font-size: 12px;
    color: #666;
    margin-top: 20px;
    border-top: 1px solid #eee;
    padding-top: 10px;
}

.notes {
    margin-top: 40px;
    padding: 15px;
    background-color: #f9f9f9;
    border-radius: 5px;
    border-left: 4px solid #ddd;
}

.notes p {
    margin: 0;
}

.footer {
    text-align: center;
}
    </style>
</head>


<body>
            <div class="header">
                <h1>Facture</h1>
                <p>N° {{ invoice.number }}</p>
            </div>
            <div class="address">
                <div class="company">
                    <p>{{ app.user.company.name|upper }}</p>
                    <p>{{ app.user.company.address1 }}</p>
                    {% if app.user.company.address2 is not empty %}
                        {{ app.user.company.address2 }}
                    {% endif %}
                    <p>{{ app.user.company.postalCode }} {{ app.user.company.city }}</p>
                </div>
                <div class="customer">
                    <p>{{ invoice.customer.name|upper }}</p>
                    <p>{{ invoice.customer.address1 }}</p>
                    {% if invoice.customer.address2 is not empty %}
                        {{ invoice.customer.address2 }}
                    {% endif %}
                    <p>{{ invoice.customer.postalCode }} {{ invoice.customer.city }}</p>
                </div>
            </div>
            <div class="date">
                <p>Date de facturation : {{ invoice.date ? invoice.date|date('d/m/Y') : '' }}</p>
                <p>Date d'echeance : {{ invoice.dueDate ? invoice.dueDate|date('d/m/Y') : '' }}</p>
                <p>Iban : {{ app.user.company.iban }}</p>
            </div>

            <div class="deliveries">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Date</th> 
                            <th>Description</th>    
                            <th>Montant</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for delivery in invoice.deliveries %}
                            <tr>
                                <td>{{ delivery.date ? delivery.date|date('d/m/Y') : '' }}</td>
                                <td>{{ delivery.description }}</td>
                                <td>{{ delivery.amount|number_format(2, ',', ' ') }} €</td>
                            </tr>
                        {% endfor %}
                    </tbody>       
                </table>
            </div>

            <div class="total">
                <p>Total HT : <span>{{ invoice.totalHT|number_format(2, ',', ' ') }} €</span></p>
                <p>TVA : <span>{{ invoice.totalTVA|number_format(2, ',', ' ') }} €</span> </p>
                <p>Total TTC : <span>{{ invoice.totalTTC|number_format(2, ',', ' ') }} €</span> </p>
            </div>

            <div class="notes">
                <p>Notes : {{ invoice.notes ? invoice.notes : 'Pas d\'observations' }}</p>
            </div>

            <div class="footer">
               SIRET :{{app.user.company.siret}}
            </div>
        
</body>
</html>